# Supabase config — copy these from supabase.com → your project → Settings → API
# Then paste the values directly into public/app.js (SUPABASE_URL and SUPABASE_KEY)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
